;; First procedure has a recursive process, follows the shape of expanding and contracting - chain of deferred `inc` operations.
;; Second procedure has an iterative process with 2 state variables. Tail-recursive.
